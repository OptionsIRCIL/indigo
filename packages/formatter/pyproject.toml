[project]
name = "formatter"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Joseph Melancon", email = "joseph@jmelancon.com" }
]
requires-python = ">=3.13"

[build-system]
requires = ["uv_build>=0.8.24,<0.9.0"]
build-backend = "uv_build"
