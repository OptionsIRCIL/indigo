[project]
name = "tests"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Kyle Holter Vogel", email = "kyle@myoptions.info" }
]
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.2",
    "selenium>=4.41.0",
]

[build-system]
requires = ["uv_build>=0.8.24,<0.9.0"]
build-backend = "uv_build"
